<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {{#each dashboards}}
          {{#if (lt @index 10)}}
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="/admin/dashboard/view/{{id_dashboard}}">{{title}}</a>
            </li>
          {{/if}}
        {{else}}
          <p>Nenhum dashboard postado</p>
        {{/each}}       
      </ul>
    </div>
  </div>
</nav>
