<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <div class="collapse navbar-collapse me-auto" id="navbarNav">
      <div class="me-auto"></div>
      <ul class="navbar-nav">
        {{#each urlsDashboard}}
        {{#if (lt @index 10)}}
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="{{url_dashboard}}" target="dashboard-iframe">{{typ_plataform_dashboard}}</a>
        </li>
        {{/if}}
        {{else}}
        <p>Nenhum dashboard postado</p>
        {{/each}}
      </ul>
    </div>
  </div>
</nav>