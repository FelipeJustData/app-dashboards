<style>
    iframe {
        width: 100%;
        height: 100vh;
        border: none;
    }
</style>

{{>_navbardashboards}}

<!--
<div class="row mt-2">
    <div class="col-8">
        <a href="/admin/dashboards"><span> Voltar</span></a>
        <h2>{{dashboard.title}}</h2>
        <p>{{dashboard.description}}</p>
    </div>
    <div class="col-4">
        <div class="row">
            <div class="col-6">
                <a href="/admin/dashboards/edit/{{dashboard.id_dashboard}}">
                    <div class="btn btn-success">Editar dashboard</div>
                </a>
            </div>
            <div class="col-6">
                <form action="/admin/dashboards/delete" method="POST">
                    <input type="hidden" name="id" value="{{dashboard.id_dashboard}}">
                    <button type="submit" class="btn btn-danger">Deletar Dashboard</button>
                </form>
            </div>
        </div>
    </div>
    <hr>
</div>
-->

<div class="row">
    {{#each url_dashboard}}
    <div class="col-12" id="content">
        <iframe name="dash-frame" title="dashboards" src="{{url_dashboard}}" frameborder="0"
            allowFullScreen="true"></iframe><br>
        <small>Data Criação: {{url_dashboard.createdAt}}</small>
        {{else }}
        <div>
            <p>Nenhum detalhe de dashboard cadastrado</p>
        </div>
    </div>
    {{/each}}

</div>